# py -m pip install requests
# py -m pip install requests==2.30.0   -> to install specific version of requests
# py -m pip install -u requests -> to install the latest version of requests
# py -m pip uninstall requests -> to uninstall requests package
# py -m pip install -r requirements.txt -> to install all the dependencies mentioned in requirements.txt file
# py -m pip freeze -> to list all the installed packages and their versions 

################################################################################################
#Venv -setup
# python -m venv venv

# to activate the venv
# .\venv\Scripts\Activate   or venv\Scripts\activate.bat     

# to deactivate the venv
# deactivate

#to create a requirements.txt file with all the installed packages and their versions
# pip freeze > requirements.txt


#git ignore file for venv
#reate a .gitignore file in the root directory of your project and add the following line to ignore the venv folder
#venv/

